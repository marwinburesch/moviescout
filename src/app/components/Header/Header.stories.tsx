import React from 'react';
import { Header } from './Header';

export default {
  title: 'component/Header',
  component: Header,
};

export const header = (): JSX.Element => <Header>Top Five</Header>;
